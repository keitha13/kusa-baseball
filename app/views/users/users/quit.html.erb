<div class="container-fluid">
  <div class="row">
    <%= render "users/users/sidebar", user: @user %>

    <div class="col-md-8 offset-md-1 d-flex align-items-center justify-content-center" id="main">
      <div>
        <h3>
          本当に退会しますか？
        </h3>

    　   <div class="text-center">
          <%= link_to "退会しない", user_path(current_user), class: 'btn btn-primary' %>
    　     <%= link_to "退会する", out_users_path, class: 'btn btn-danger', method: :put, data: {confirm: '本当によろしいですか？'} %>
    　   </div>
      </div>
    </div>
  </div>
</div>

<%= render "users/users/footer" %>